# Frontend Authentication System

Полнофункциональная система аутентификации для Nuxt 4 приложения, интегрированная с Laravel auth-service.

## 🚀 Возможности

- **Регистрация** с валидацией пароля и подтверждением
- **Вход** с проверкой email и пароля
- **Выход** с отзывом токенов
- **Автоматическое обновление токенов** (refresh token)
- **Защита маршрутов** через middleware
- **Валидация паролей** согласно StrongPassword правилам
- **Современный UI/UX** с анимациями и индикаторами

## 📁 Структура файлов

```
src/app/
├── middleware/
│   ├── auth.ts                        # Защита аутентифицированных маршрутов
│   └── guest.ts                       # Редирект для гостевых страниц
├── pages/
│   ├── index.vue                      # Главная страница с auth состоянием
│   ├── login.vue                      # Страница входа
│   └── register.vue                   # Страница регистрации
├── plugins/
│   └── auth.client.ts                 # Инициализация auth store
└── stores/
    └── auth.store.ts                  # Pinia store для аутентификации
```

## 🔧 Настройка

### 1. Переменные окружения

Скопируйте `env.example` в `.env`:

```bash
cp env.example .env
```

Или создайте `.env` файл:

```env
# API Configuration
NUXT_PUBLIC_API_BASE_URL=http://localhost:8000

# App Configuration
NUXT_PUBLIC_APP_NAME=SkillUp
NUXT_PUBLIC_APP_URL=http://localhost:3000

# Development
NODE_ENV=development
```

### 2. Конфигурация Nuxt

В `nuxt.config.ts` уже настроены необходимые модули и проксирование:

```typescript
export default defineNuxtConfig({
  modules: ['@pinia/nuxt'],
  
  // Проксирование API запросов в development
  nitro: {
    devProxy: {
      '/api': {
        target: 'http://localhost:8000/api',
        changeOrigin: true,
        prependPath: true
      }
    }
  }
})
```

### 3. Запуск приложения

1. **Запустите Laravel backend** (порт 8000):
   ```bash
   cd backend
   php artisan serve
   ```

2. **Запустите Nuxt frontend** (порт 3000):
   ```bash
   cd frontend/src
   npm run dev
   ```

3. **Откройте браузер**: http://localhost:3000

## 🎯 Использование

### Auth Store

```typescript
import { useAuthStore } from '~/stores/auth.store'

const auth = useAuthStore()

// Состояние
auth.isAuthenticated    // boolean
auth.user              // User | null
auth.loading           // boolean
auth.error             // string

// Методы
await auth.login(email, password)
await auth.register(name, email, password, passwordConfirmation)
await auth.logout()
await auth.initialize() // Инициализация при загрузке приложения
```

### Middleware

```typescript
// Защита маршрутов (только для аутентифицированных)
definePageMeta({
  middleware: 'auth'
})

// Гостевые маршруты (редирект если уже авторизован)
definePageMeta({
  middleware: 'guest'
})
```

### Валидация паролей

Store включает встроенную валидацию паролей:

```typescript
const passwordErrors = auth.validatePassword(password)
// Возвращает массив ошибок или пустой массив

const isValidEmail = auth.validateEmail(email)
// Возвращает boolean
```

## 🔐 API Endpoints

Система интегрирована с Laravel auth-service:

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход
- `POST /api/auth/refresh` - Обновление токенов
- `GET /api/auth/me` - Получение текущего пользователя

## 🎨 UI Компоненты

### Страницы аутентификации

- **login.vue** - Современная форма входа с валидацией
- **register.vue** - Регистрация с индикатором силы пароля

## 🔄 Автоматические функции

1. **Инициализация** - Auth store автоматически инициализируется при загрузке
2. **Refresh токены** - Автоматическое обновление при истечении access token
3. **Редиректы** - Автоматические редиректы на основе статуса аутентификации
4. **Очистка состояния** - Автоматическая очистка при выходе

## 🛡️ Безопасность

- Токены хранятся в localStorage
- Автоматическая очистка при выходе
- Валидация на клиенте и сервере
- Защита от CSRF через SameSite cookies
- Rate limiting на уровне API

## 📱 Адаптивность

Все компоненты полностью адаптивны и работают на:
- Десктопе (1200px+)
- Планшетах (768px - 1199px)
- Мобильных устройствах (< 768px)

## 🚨 Обработка ошибок

Система включает комплексную обработку ошибок:

- Валидация форм в реальном времени
- Отображение ошибок сервера
- Автоматическая очистка ошибок
- Логирование для отладки

## 🔧 Разработка

### Запуск в режиме разработки

```bash
npm run dev
```

### Сборка для продакшена

```bash
npm run build
```

### Линтинг

```bash
npm run lint
```

## 📝 Примечания

- Все тексты на русском языке
- Использует TypeScript для типобезопасности
- Совместим с Nuxt 4 и Vue 3
- Следует принципам Composition API
- Оптимизирован для производительности
