# Issue: Сохранение данных пользователя после входа/регистрации и переход на главную страницу

## 🎯 Описание
После успешного входа или регистрации необходимо:
1. Сохранить данные пользователя в store/state
2. Перенаправить пользователя на главную страницу
3. Обеспечить сохранение сессии при перезагрузке страницы

## 🐛 Текущая проблема
- После входа/регистрации пользователь остается на той же странице
- Данные пользователя не сохраняются в глобальном состоянии
- При перезагрузке страницы пользователь теряет авторизацию

## ✅ Критерии приемки

### 1. Сохранение данных пользователя
- [ ] После успешного входа данные пользователя сохраняются в Pinia store
- [ ] После успешной регистрации данные пользователя сохраняются в Pinia store
- [ ] Токены доступа сохраняются в localStorage
- [ ] Состояние `isAuthenticated` обновляется на `true`

### 2. Перенаправление на главную страницу
- [ ] После входа пользователь перенаправляется на `/` (главная страница)
- [ ] После регистрации пользователь перенаправляется на `/` (главная страница)
- [ ] URL в браузере обновляется корректно

### 3. Сохранение сессии
- [ ] При перезагрузке страницы пользователь остается авторизованным
- [ ] Данные пользователя восстанавливаются из localStorage
- [ ] Токены автоматически обновляются при необходимости

### 4. UI обновления
- [ ] В header отображается имя пользователя
- [ ] Кнопка "Войти" заменяется на "Выйти"
- [ ] Показывается статус авторизации

## 🔧 Технические детали

### Файлы для изменения:
- `frontend/src/app/stores/auth.store.ts` - логика сохранения данных
- `frontend/src/app/pages/login.vue` - обработка успешного входа
- `frontend/src/app/pages/register.vue` - обработка успешной регистрации
- `frontend/src/app/layouts/default.vue` - отображение состояния пользователя
- `frontend/src/app/middleware/auth.ts` - проверка авторизации

### Методы для реализации:
```typescript
// В auth store
const saveUserData = (userData: User, tokens: AuthTokens) => {
  user.value = userData
  accessToken.value = tokens.access_token
  refreshToken.value = tokens.refresh_token
  localStorage.setItem('user', JSON.stringify(userData))
  localStorage.setItem('access_token', tokens.access_token)
  localStorage.setItem('refresh_token', tokens.refresh_token)
}

const redirectToHome = () => {
  router.push('/')
}
```

### Проверка авторизации при загрузке:
```typescript
// В app.vue или middleware
const initializeAuth = async () => {
  const token = localStorage.getItem('access_token')
  if (token) {
    try {
      const userData = await getCurrentUser()
      // Восстанавливаем состояние
    } catch (error) {
      // Очищаем невалидные токены
      clearTokens()
    }
  }
}
```

## 🎨 UI/UX требования

### Главная страница должна показывать:
- Приветствие с именем пользователя
- Кнопку "Выйти" в header
- Информацию о последнем входе
- Возможность перейти в профиль

### Header должен содержать:
- Логотип/название сайта
- Имя пользователя (если авторизован)
- Кнопку "Выйти" (если авторизован)
- Кнопки "Войти"/"Регистрация" (если не авторизован)

## 🧪 Тестирование

### Сценарии тестирования:
1. **Успешный вход:**
   - Ввести корректные данные
   - Нажать "Войти"
   - Проверить перенаправление на главную
   - Проверить сохранение данных в store

2. **Успешная регистрация:**
   - Заполнить форму регистрации
   - Нажать "Зарегистрироваться"
   - Проверить перенаправление на главную
   - Проверить сохранение данных в store

3. **Перезагрузка страницы:**
   - Авторизоваться
   - Обновить страницу (F5)
   - Проверить, что пользователь остается авторизованным

4. **Выход:**
   - Нажать "Выйти"
   - Проверить очистку данных
   - Проверить перенаправление на страницу входа

## 📋 Дополнительные задачи

- [ ] Добавить loading состояния при авторизации
- [ ] Добавить обработку ошибок с понятными сообщениями
- [ ] Добавить валидацию токенов при инициализации
- [ ] Добавить автоматическое обновление токенов
- [ ] Добавить middleware для защиты приватных страниц

## 🏷️ Labels
- `enhancement`
- `auth`
- `frontend`
- `high-priority`

## 👤 Assignee
@renzo-dev

## 📅 Milestone
v1.0.0 - Базовая аутентификация
